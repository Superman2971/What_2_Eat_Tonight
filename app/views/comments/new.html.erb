<h1>All of the comments</h1>

<% @comments.each do |c| %>
  <div>
    <div><%= c.text %></div>
    <div><%= link_to "Edit", edit_comment_path(c) %></div>
  </div>
</br>
<% end %>

<%= form_for @comment do |f| %>
  <%= f.text_field :text, placeholder: "Text Here..." %>
  <%= f.submit %>
<% end %>
